import { LocomotiveScrollDirective } from './locomotive-scroll.directive';

describe('LocomotiveScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new LocomotiveScrollDirective();
    expect(directive).toBeTruthy();
  });
});
